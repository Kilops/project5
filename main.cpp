#include <windows.h>
#include <locale>
int plus(int a, int b);
int check();

int main(double a, double b)
{
	c = plus(a,b)
	return c1;
}

int plus(int a, int b)
{
	a = check();
	b = check();
	c = a + b;
}

int check()
{
	while (1)
	{
		printf("Enter data:  ");
		scanf_s("%c", &el);
		if (isalpha(el) != 0 and isalnum(el))
		{
			return el;
			break;
		}
	}
}